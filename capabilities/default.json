{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Permissions granted to the main webview window: the core invoke surface and our project-specific commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default"
  ]
}
